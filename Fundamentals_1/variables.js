//==== Variable & values ====
console.log("==== Variable & values ====");

// let new = "can't use variable like this keywords are reserve in JS" 
// let function = "Same can use names like this keywords are reserve in JS"
let $function = "can name variable like this use $ sign as function like keyword are reserve in JS"
console.log('$function', $function)

const fetch_Value = 10;
// variable firstname must be small => It should follow camelcase order => Can name like ex-let fetch_Value, let myFirstJob

